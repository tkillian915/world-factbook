export class infoCardModel
{
    img: string;
    title: string;
    content: string;

    constructor(img: string, title: string, content: string)
    {
        this.img = img;
        this.title = title;
        this.content = content;
    }
}