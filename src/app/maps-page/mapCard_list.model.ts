export class MapCardModel
{
    img: string;
    title: string;
    text: string;

    constructor(img: string, title: string, text: string)
    {
        this.img = img;
        this.title = title;
        this.text = text;
    }
}